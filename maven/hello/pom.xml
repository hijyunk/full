<project>
    <groupId>com.moon</groupId>
    <artifactId>HelloWorld</artifactId>    <!-- // product id 같은 거..? -->
    <modelVersion>4.0.0</modelVersion>  <!-- // 정해져있음 바꾸면 안됨 -->
    <version>1.0</version>
    <packaging>jar</packaging>
    <build>
        <plugins>
            <plugin>    <!-- // maven compile에 해당 됨 -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.7.0</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
            <plugin>    <!-- // maven package에 해당 됨  -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.0.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>HelloWorld</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>